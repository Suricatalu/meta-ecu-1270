# rauc_env.cfg — mounted ONLY when RAUC_ENABLED=1, AFTER board-base.cfg,
# so this override wins. Points default bootcmd at the RAUC A/B env script.
CONFIG_BOOTCOMMAND="i2c dev 1; i2c mw 0x30 0x20 0x9c; run rauc_bootcmd;"
